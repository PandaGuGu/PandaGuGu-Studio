# PandaGuGu Studio 环境变量示例
# 复制为 .env 使用（.env 已被 gitignore，严禁提交真实 Key）

# 构建路径（部署到子目录时用，配合 build:gh 脚本）
# VITE_BASE=/vcanvas/

# 预留：未来如需自定义 API 代理地址
# VITE_API_BASE=

# 说明：模型 API Key 不在此配置。
# 用户在应用内右上角「模型 ⚙」设置页填写，保存在浏览器 localStorage。

# pgg CLI 的环境变量（优先级：命令行参数 > 环境变量 > ~/.pandagugu.json）
# PGG_PROVIDER=zai            # zai|deepseek|kimi|qwen|ark|google|fireworks|openrouter|custom
# PGG_MODEL=qwen-plus
# PGG_API_KEY=sk-xxx
# PGG_ENDPOINT=               # 自定义端点（custom 服务商必填）
